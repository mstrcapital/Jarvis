version: '3.8'

services:
  # SearXNG - Default settings, no custom config
  searxng:
    image: searxng/searxng:latest
    container_name: searxng
    ports:
      - "8888:8080"
    environment:
      - SEARXNG_SECRET=tavily-search-key-2026
    restart: unless-stopped